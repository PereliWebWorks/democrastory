<%= form_for(child_story) do |f| %>
  <% if child_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% child_story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :content, size: "60x10" %>
  </div>



  <div class="actions">
    <%= f.hidden_field :parent_id, value: child_story.parent_id %>
    <%= f.submit %>
  </div>
<% end %>