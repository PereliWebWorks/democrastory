<p id="notice"><%= notice %></p>

<h1>Stories</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Parent</th>
      <th>Child</th>
      <th>User</th>
      <th>Upvotes</th>
      <th>Published</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stories.each do |story| %>
      <tr>
        <td><%= story.content %></td>
        <td><%= story.parent_id %></td>
        <td><%= story.child_id %></td>
        <td><%= story.user_id %></td>
        <td><%= story.upvotes %></td>
        <td><%= story.published %></td>
        <td><%= link_to 'Show', story %></td>
        <td><%= link_to 'Edit', edit_story_path(story) %></td>
        <td><%= link_to 'Destroy', story, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Story', new_story_path %>
